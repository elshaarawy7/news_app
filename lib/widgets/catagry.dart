import 'package:flutter/material.dart';

class CatagryCard extends StatelessWidget {
  const CatagryCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}